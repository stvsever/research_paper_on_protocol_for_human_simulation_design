services:
  paper-template:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../:/workspace
    working_dir: /workspace
    command: ["paper-template", "run-all"]
